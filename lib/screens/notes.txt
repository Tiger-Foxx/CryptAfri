dans le onpress de mon bouton addToCart j'aimerai faire une fonction qui dans mon Firestore dans la collection CartItems cree un document avec comme attributs : UserEmail (qui correspond a l'mail de l'utilisateur courant) et IDProduit qui est l'identifiant du produit qu'il souhaite ajouter , ne t'inquite pas dans on code l'identifiant de ce produit est dans la variable product.ID que j'ai recuperé dans le build avec :  final product = ModalRoute.of(context)!.settings.arguments as ProductModel;  c'est l'adresse mail de l'utilisateur firebaseAuth courant que je ne sais pas comment recuperer d'autant plus que dans mon application il ya deux mode de connexion soit par nom d'utilisateur ou mot de poasse soit via google, je t'en pris explique moi comment faire dans le onpress de ce bouton addtocart, surtout que la j'ai juste fais une collection 'CartItems' pour que ensuite dans mon panier je recupere tous les CartItems dont tous les UserEmail correspondent à l'UserEmail de l'utilisateur courant  